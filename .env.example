# ─────────────────────────────────────────────────────────────────────────
# Agentry — Environment Variables
# Copy to .env and fill in real values. Never commit .env.
#
#   cp .env.example .env
# ─────────────────────────────────────────────────────────────────────────

# ── Model provider ───────────────────────────────────────────────────────────
# "gemini" (default) or "bedrock". Only one is active per run.
MODEL_PROVIDER=gemini

# ── Gemini (used when MODEL_PROVIDER=gemini) ────────────────────────────────
GEMINI_API_KEY=

# ── AWS Bedrock (used when MODEL_PROVIDER=bedrock) ──────────────────────────
# Credentials are resolved by boto3's normal chain — these env vars, or a
# named profile / IAM role if you'd rather use one of those instead.
# You also need to request access to your chosen model under
# AWS Console -> Bedrock -> Model access, in this same region, before it
# will actually respond.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# ── Telegram (notify_user tool) ────────────────────────────────────────────
# Create a bot via @BotFather on Telegram -> /newbot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Storefront login ────────────────────────────────────────────────────────
# No env var needed here — log in once via `python scripts/capture_session.py`
# (opens a real browser window for you to log into manually) and every
# storefront tool reuses that saved session automatically. See README.

# ── Budget cap (check_budget tool) ──────────────────────────────────────────
# Weekly grocery spend cap, in the storefront's currency (e.g. INR).
# Defaults to 1500 if unset.
WEEKLY_BUDGET_INR=1500
